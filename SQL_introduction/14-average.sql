-- This script calculates the average score of all records in the table "second_table".
-- It uses the SELECT statement with the AVG() function to compute the average of the scores.
SELECT AVG(score) AS average
FROM second_table;
