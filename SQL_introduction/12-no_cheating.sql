-- Script to update the score of the record with name='Bob' to 10 in the table "second_table".
-- The query utilizes the UPDATE statement to modify the score field.
UPDATE second_table
SET score = 10
WHERE name = 'Bob';
