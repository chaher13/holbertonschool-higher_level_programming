-- Query to retrieve the count of records with id=89 in the table "first_table".
-- The query utilizes the COUNT(*) function to calculate the number of occurrences.
SELECT COUNT(*)
FROM first_table
WHERE id = 89;
